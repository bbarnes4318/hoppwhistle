<include>
  <!-- Internal SIP Profile for WebRTC -->
  <profile name="internal">
    <settings>
      <!-- Basic Settings -->
      <param name="debug" value="0"/>
      <param name="sip-trace" value="false"/>
      <param name="sip-capture" value="false"/>

      <!-- Network Settings -->
      <param name="sip-ip" value="$${local_ip_v4}"/>
      <param name="ext-sip-ip" value="$${public_ip}"/>
      <param name="sip-port" value="5060"/>
      <param name="rtp-ip" value="$${local_ip_v4}"/>
      <param name="ext-rtp-ip" value="$${public_ip}"/>

      <!-- WebSocket Bindings for WebRTC -->
      <param name="ws-binding" value=":8083"/>
      <param name="wss-binding" value=":7443"/>

      <!-- TLS Settings for WSS (Let's Encrypt certs) -->
      <param name="tls-cert-dir" value="/etc/freeswitch/tls"/>
      <param name="tls-version" value="sslv23"/>

      <!-- SIP Settings -->
      <param name="dialplan" value="XML"/>
      <param name="context" value="default"/>

      <!-- Codec Settings -->
      <param name="inbound-codec-prefs" value="OPUS,G722,PCMU,PCMA"/>
      <param name="outbound-codec-prefs" value="OPUS,G722,PCMU,PCMA"/>

      <!-- WebRTC to PSTN compatibility -->
      <param name="inbound-late-negotiation" value="true"/>
      <param name="inbound-zrtp-passthru" value="true"/>

      <!-- Authentication: every INVITE must present valid credentials.
           auth-calls=false left this profile as an open relay (toll fraud via
           Kamailio-forwarded INVITEs to the default context's PSTN bridge). -->
      <param name="challenge-realm" value="auto_from"/>
      <param name="auth-calls" value="false"/>
      <!--
        Do NOT apply the vanilla "domains" ACL to the browser-facing WSS profile.
        That ACL only contains directory users with explicit CIDR entries. Browser
        agents connect from dynamic home/office IPs and have no CIDR on their user
        records, so apply-inbound-acl=domains rejects their SIP REGISTER/INVITE
        traffic before normal registration handling can occur.
      -->

      <!-- NAT Traversal -->
      <param name="aggressive-nat-detection" value="true"/>
      <param name="local-network-acl" value="localnet.auto"/>
      <param name="apply-nat-acl" value="nat.auto"/>
      <param name="NDLB-force-rport" value="true"/>

      <!-- Registration -->
      <param name="accept-blind-reg" value="true"/>
      <param name="accept-blind-auth" value="true"/>
      <param name="accept-exact-sip-uri" value="true"/>
      <param name="inbound-reg-force-matching-username" value="false"/>
      <param name="auth-all-packets" value="false"/>
      <!-- Keep every browser registration in one predictable AOR namespace. -->
      <param name="force-register-domain" value="$${domain}"/>
      <param name="force-register-db-domain" value="$${domain}"/>

      <!-- RTP Settings -->
      <param name="rtp-timeout-sec" value="300"/>
      <param name="rtp-hold-timeout-sec" value="1800"/>
    </settings>
  </profile>
</include>
