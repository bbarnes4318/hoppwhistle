/**
 * Screens that are built but switched off.
 *
 * Flip a flag to `true` to bring its screen back: the nav entry reappears and
 * the route stops redirecting. Nothing else about the screen was removed.
 */

/**
 * An agent's own pay (`/payroll`, "My payroll").
 *
 * Off for now. The page and its API are intact; the link is hidden from the
 * agent sidebar and the URL redirects to the dashboard.
 */
export const MY_PAYROLL_ENABLED = false;
