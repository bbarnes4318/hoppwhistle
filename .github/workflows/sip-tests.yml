# DISABLED - Re-enable when SIP infrastructure is stable
# name: SIP Tests
#
# on:
#   push:
#     branches: [main, develop]
#   pull_request:
#     branches: [main, develop]
#   workflow_dispatch:

name: SIP Tests (DISABLED)

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  sip-tests:
    if: false  # Always skip
    runs-on: ubuntu-latest
    steps:
      - run: echo "SIP tests disabled"
