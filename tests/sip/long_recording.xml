<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Long Recording Edge Case Test
  
  Tests:
  - Call establishment with recording enabled
  - Long-duration call (5+ minutes) to test recording handling
  - Proper call teardown after long recording
  - Edge cases: recording limits, storage handling
-->
<scenario name="Long Recording Edge Case">
  <!-- Send INVITE for long recording test -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: LongCallTest <sip:longcall@[local_ip]:[local_port]>;tag=[call_number]
      To: RecordingTest <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:longcall@[local_ip]:[local_port]>
      Max-Forwards: 70
      User-Agent: SIPp-LongRecording-Test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=Long Recording Test Call
      c=IN IP[local_ip_type] [local_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8 18
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:18 G729/8000
      a=sendrecv
    ]]>
  </send>

  <!-- Wait for 100 Trying (optional) -->
  <recv response="100" optional="true">
  </recv>

  <!-- Wait for 180 Ringing (optional) -->
  <recv response="180" optional="true">
  </recv>

  <!-- Wait for 200 OK -->
  <recv response="200" rtd="true">
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: LongCallTest <sip:longcall@[local_ip]:[local_port]>;tag=[call_number]
      To: RecordingTest <sip:[service]@[remote_ip]:[remote_port]>;tag=[peer_tag]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Long call duration: 5 minutes (300000ms) -->
  <!-- This tests recording handling for long calls -->
  <pause milliseconds="300000"/>

  <!-- Send periodic re-INVITE to keep call alive (optional, for very long calls) -->
  <!-- SIPp will handle this automatically if needed -->

  <!-- Send BYE to end long call -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: LongCallTest <sip:longcall@[local_ip]:[local_port]>;tag=[call_number]
      To: RecordingTest <sip:[service]@[remote_ip]:[remote_port]>;tag=[peer_tag]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Wait for 200 OK to BYE -->
  <recv response="200" rtd="true">
  </recv>

  <!-- Test completed successfully -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="60000, 120000, 180000, 240000, 300000, 360000"/>
</scenario>

