<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Buyer Failover Success - Second Buyer Accepts
  
  This scenario simulates the second buyer endpoint accepting the call
  after the first buyer failed.
-->
<scenario name="Buyer Failover - Second Buyer Success">
  <!-- Receive INVITE from Hopwhistle (after first buyer failed) -->
  <recv request="INVITE" rtd="true">
  </recv>

  <!-- Send 100 Trying -->
  <send>
    <![CDATA[
      SIP/2.0 100 Trying
      Via: [via]
      From: [from]
      To: [to]
      Call-ID: [call_id]
      CSeq: [cseq]
      Content-Length: 0
    ]]>
  </send>

  <!-- Send 180 Ringing -->
  <send>
    <![CDATA[
      SIP/2.0 180 Ringing
      Via: [via]
      From: [from]
      To: [to];tag=[call_number]
      Call-ID: [call_id]
      CSeq: [cseq]
      Contact: <sip:buyer2@[local_ip]:[local_port]>
      Content-Length: 0
    ]]>
  </send>

  <!-- Wait a bit before answering -->
  <pause milliseconds="1000"/>

  <!-- Send 200 OK with SDP -->
  <send>
    <![CDATA[
      SIP/2.0 200 OK
      Via: [via]
      From: [from]
      To: [to];tag=[call_number]
      Call-ID: [call_id]
      CSeq: [cseq]
      Contact: <sip:buyer2@[local_ip]:[local_port]>
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=buyer2 53655766 2353687638 IN IP[local_ip_type] [local_ip]
      s=Buyer2 Answer
      c=IN IP[local_ip_type] [local_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8 18
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:18 G729/8000
      a=sendrecv
    ]]>
  </send>

  <!-- Receive ACK -->
  <recv request="ACK" rtd="true">
  </recv>

  <!-- Hold call for a few seconds (simulating conversation) -->
  <pause milliseconds="5000"/>

  <!-- Send BYE to end call -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: Buyer2 <sip:buyer2@[local_ip]:[local_port]>;tag=[call_number]
      To: [last_To]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Receive 200 OK to BYE -->
  <recv response="200" rtd="true">
  </recv>

  <!-- Test completed successfully -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>
</scenario>

